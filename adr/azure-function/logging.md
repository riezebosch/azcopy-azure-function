Redirect to `/tmp` on `tmpfs` so files are automatically removed after the process finishes.

Set logging-level to `ERROR`, `FATAL` or `NONE` because of disk space issues.

Console output is written to Log Analytics.